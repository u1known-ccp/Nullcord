dist
node_modules
.VSCodeCounter
.vars
.secrets
.input

*.exe
vencord_installer
equicord_installer

.idea
.DS_Store

yarn.lock
bun.lock
package-lock.json

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*
*.tsbuildinfo

.modules/
src/userplugins

ExtensionCache/
/settings

# vencord companion module cache
.modules

# wakatime related stuff
.wakatime-project

# Claude Code local settings (personal, per-user - not committed)
.claude/settings.local.json

# Standalone analytics backend (separate Cloudflare Worker infra, deployed by hand - not part of the public build)
analytics-worker/
